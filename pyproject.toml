[project]
name = "skill-aider"
version = "0.1.0"
description = "Code editing skill for Kiso using aider"
requires-python = ">=3.11"
dependencies = [
    "aider-chat>=0.82",
]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
